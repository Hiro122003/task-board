# CLAUDE.md

このファイルは、リポジトリ内のコードを操作する際に Claude Code (claude.ai/code) へ提供するガイダンスです。

## プロジェクト概要

タスクボードアプリケーション。現在は初期状態のため、技術スタックと構成が確定したらこのセクションを更新してください。

## Git 運用ルール

コードを変更するたびに、`main` ブランチへコミットしてプッシュすること。

```powershell
git add <変更ファイル>
git commit -m "<メッセージ>"
git push origin main
```
